<Window x:Class="Al25.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Al25.Views.Windows"
        xmlns:fa5="http://schemas.fontawesome.com/icons/"
        xmlns:cmd ="clr-namespace:Al25.Infrastuctura.Commands"
        xmlns:oxy ="http://oxyplot.org/wpf"
        xmlns:vm ="clr-namespace:Al25.ViewModels"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="800">

    <Window.Resources>
        <cmd:CloseApplicationCommand x:Key="CloseApplicationCommand"/>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Modifiers="Control" Key="Q" Command="{StaticResource CloseApplicationCommand}"></KeyBinding>
        <KeyBinding Modifiers="Control" Key="Right" Command="{Binding ChangeTabIndexCommand}" CommandParameter="+1"></KeyBinding>
        <KeyBinding Modifiers="Control" Key="Left" Command="{Binding ChangeTabIndexCommand}" CommandParameter="-1"></KeyBinding>
    </Window.InputBindings>
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File">
                <!--MenuItem Header="Exit" Command="{Binding CloseApplicationCommand}"/-->
                <MenuItem Header="Exit" Command="{StaticResource CloseApplicationCommand}"/>
                          
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="Info"/>
            </MenuItem>

        </Menu>

        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Text="{Binding Status}"></TextBlock>
            </StatusBarItem>
            <StatusBarItem>
                <ProgressBar Width="120" Height="16" Value="30"></ProgressBar>
            </StatusBarItem>
        </StatusBar>

        <TabControl SelectionChanged="TabControl_SelectionChanged" SelectedIndex="{Binding SelectedPageIndex}">
            <TabItem Header ="Студенты" >
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="250">
                        </ColumnDefinition>
                    </Grid.ColumnDefinitions> 

                    <GroupBox Header="Группы">
                        <ListBox ItemsSource="{Binding Groups}"></ListBox>
                    </GroupBox>
                </Grid>
            </TabItem>
            <TabItem Header="График">
                <oxy:PlotView Model="{Binding MyPlotModel}"/>
            </TabItem>
            <Grid Height="388" VerticalAlignment="Bottom" Width="794"/>
        </TabControl>
    </DockPanel>
</Window>
